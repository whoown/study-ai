# 多轮指代消解对话系统依赖包
# Multi-turn Coreference Resolution Dialogue System Dependencies

# 核心数据处理包（已验证）
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# OpenAI API（已验证）
openai>=1.0.0

# Web框架（已验证）
fastapi>=0.100.0
uvicorn>=0.23.0

# 异步处理（已验证）
aiofiles>=23.0.0

# 图像处理（已验证）
Pillow>=10.0.0

# HTTP请求（已验证）
requests>=2.31.0

# JSON处理（已验证）
orjson>=3.9.0

# 环境变量管理（已验证）
python-dotenv>=1.0.0

# 日志（已验证）
loguru>=0.7.0

# 开发和测试（已验证）
pytest>=7.0.0
pytest-asyncio>=0.21.0

# 监控和日志（测试中使用）
prometheus-client>=0.17.0
structlog>=23.0.0
python-json-logger>=2.0.0

# 类型检查（已验证）
mypy>=1.0.0

# 代码格式化（已验证）
black>=23.0.0
flake8>=6.0.0

# 可选：高级NLP工具（可能需要编译）
# LangChain核心包
# langchain>=0.0.350
# langchain-openai>=0.0.5

# NLP工具
spacy>=3.7.0

# spaCy语言模型（中文NLP必需）
# zh-core-web-sm==3.8.0  # 需要单独安装
# en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.0/en_core_web_sm-3.7.0-py3-none-any.whl

# 缓存和数据库（可能需要编译）
# redis>=4.5.0
# aioredis>=2.0.0

# 图像处理（可能需要编译）
# opencv-python>=4.8.0

# 深度学习框架（可选，体积较大）
# torch>=2.0.0
# torchvision>=0.15.0
# transformers>=4.30.0

# 可选：指代消解专用库
# neuralcoref>=4.0  # 注意：可能与新版spaCy不兼容
# allennlp>=2.10.0  # 用于更高级的NLP任务

# 其他开发工具
# jupyter>=1.0.0